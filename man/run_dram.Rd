% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{run_dram}
\alias{run_dram}
\title{Run Delayed Rejection Adaptive Metropolis}
\usage{
run_dram(
  target_density = NULL,
  initial_state,
  n_iter,
  initial_proposal_covariance = diag(length(initial_state)),
  adapt_start = 50L,
  delayed_rejection_scale = 0.5,
  epsilon = 1e-08,
  regularization = NULL,
  log_target = NULL
)
}
\arguments{
\item{target_density}{Function of one numeric state vector \code{x} that returns
the target density \code{pi(x)}.}

\item{initial_state}{Numeric vector giving the starting value of the chain.}

\item{n_iter}{Number of Metropolis updates to run.}

\item{initial_proposal_covariance}{Initial covariance matrix for the
first-stage proposal.}

\item{adapt_start}{Iteration at which AM-style covariance learning is first
allowed to update the proposal.}

\item{delayed_rejection_scale}{Number \code{gamma} in \verb{(0, 1]} that shrinks the
first-stage covariance for the second-stage rescue proposal.}

\item{epsilon}{Small diagonal term in the AM-style update
\verb{S_n = (1 / n) sum_i (theta^(i) - theta_bar)(theta^(i) - theta_bar)^T + epsilon I}.}

\item{regularization}{Deprecated compatibility alias for \code{epsilon}.}

\item{log_target}{Deprecated compatibility alias. If supplied, the wrapper
converts \code{log_target(x)} to \code{exp(log_target(x))}.}
}
\value{
An object of class \code{"adaptive_mcmc_run"}.
}
\description{
This is a presentation-friendly alias for \code{\link[=dram]{dram()}}.
}
