% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{basic_metropolis}
\alias{basic_metropolis}
\title{Run Basic Metropolis}
\usage{
basic_metropolis(
  target_density = NULL,
  initial_state,
  n_iter,
  proposal_covariance = diag(length(initial_state)),
  log_target = NULL
)
}
\arguments{
\item{target_density}{Function of one numeric state vector \code{x} that returns
the target density \code{pi(x)}.}

\item{initial_state}{Numeric vector giving the starting value of the chain.}

\item{n_iter}{Number of Metropolis updates to run.}

\item{proposal_covariance}{Covariance matrix \code{S} in the Gaussian random-walk
proposal \verb{x^* = x + N(0, S)}.}

\item{log_target}{Deprecated compatibility alias. If supplied, the wrapper
converts \code{log_target(x)} to \code{exp(log_target(x))}.}
}
\value{
An object of class \code{"adaptive_mcmc_run"}.
}
\description{
This is the fixed-kernel random-walk Metropolis baseline. The R wrapper
checks the inputs, calls \code{basic_metropolis_cpp()}, and then adds the common
package metadata used by the plotting and summary helpers.
}
\examples{
target <- make_correlated_gaussian_target()
run <- basic_metropolis(
  target_density = target$density,
  initial_state = target$recommended_initial,
  n_iter = 200,
  proposal_covariance = diag(2)
)
head(run$draws)

}
