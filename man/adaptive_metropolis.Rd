% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{adaptive_metropolis}
\alias{adaptive_metropolis}
\title{Run Adaptive Metropolis}
\usage{
adaptive_metropolis(
  target_density = NULL,
  initial_state,
  n_iter,
  initial_proposal_covariance = diag(length(initial_state)),
  adapt_start = 50L,
  epsilon = 1e-08,
  regularization = NULL,
  log_target = NULL
)
}
\arguments{
\item{target_density}{Function of one numeric state vector \code{x} that returns
the target density \code{pi(x)}.}

\item{initial_state}{Numeric vector giving the starting value of the chain.}

\item{n_iter}{Number of Metropolis updates to run.}

\item{initial_proposal_covariance}{Initial covariance matrix \code{S_0} used
before AM has enough history to adapt.}

\item{adapt_start}{Iteration at which AM is first allowed to replace \code{S_0}
with the empirical covariance estimate built from past draws.}

\item{epsilon}{Small diagonal term in the AM update
\verb{S_n = (1 / n) sum_i (theta^(i) - theta_bar)(theta^(i) - theta_bar)^T + epsilon I}.}

\item{regularization}{Deprecated compatibility alias for \code{epsilon}.}

\item{log_target}{Deprecated compatibility alias. If supplied, the wrapper
converts \code{log_target(x)} to \code{exp(log_target(x))}.}
}
\value{
An object of class \code{"adaptive_mcmc_run"}.
}
\description{
Adaptive Metropolis (AM) begins with a fixed pilot covariance and then learns
an empirical covariance from the realised chain. The R wrapper checks the
inputs, calls \code{adaptive_metropolis_cpp()}, and returns the result in the
standard package format.
}
\examples{
target <- make_correlated_t_target()
run <- adaptive_metropolis(
  target_density = target$density,
  initial_state = target$recommended_initial,
  n_iter = 300,
  initial_proposal_covariance = diag(2),
  adapt_start = 50
)
tail(run$adaptation_magnitude)

}
